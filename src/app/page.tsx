import { HomeExperience } from "@/components/trip-compass/home-experience";

/**
 * Renders the anonymous-first Trip Compass home experience.
 * @returns Home page UI
 */
export default function Home() {
  return <HomeExperience />;
}
