# 한눈에 보기

- 추천 결과 직전의 `생성 중` 전용 화면은 이후 소형 광고 슬롯을 붙일 수 있는 지면으로 유지한다.
- 광고는 결과 순위나 자연 추천 카드와 섞지 않고, 생성 중 화면 안에서만 다룬다.

## 작업 계획

1. 생성 중 화면을 결과 화면과 분리된 별도 단계로 유지한다.
2. 광고는 로딩바 위 또는 옆의 소형 슬롯으로만 배치한다.
3. `Sponsored` 또는 `Partner` 표기를 명확히 넣는다.
4. 나중에 실제 광고 연동을 하더라도 추천 순위나 카드 순서를 건드리지 않게 유지한다.
