# 한눈에 보기

- DB 스키마를 기준으로 검토용 ERD 문서를 만든다.
- 논리명과 물리명을 함께 적고, 실제 FK 관계와 FK가 없는 참조를 분리해 표시한다.

## 계획

1. `src/lib/db/schema.ts`와 `drizzle/*.sql`을 대조한다.
2. 실제 테이블과 관계를 기준으로 ERD를 작성한다.
3. 검토 포인트가 되는 비정규화/비-FK 참조를 메모한다.
