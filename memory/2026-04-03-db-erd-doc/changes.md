# 한눈에 보기

- `docs/db-erd.md`를 새로 만들었다.
- 논리명/물리명 테이블 목록, Mermaid ERD, 핵심 컬럼 대응표를 함께 정리했다.

## 변경 사항

1. `docs/db-erd.md`
2. 인증, 추천 마스터, 사용자 여행 데이터 3개 영역으로 구조를 정리했다.
3. `user_future_trips.source_snapshot_id`처럼 의미상 참조하지만 FK가 없는 컬럼은 검토 메모로 분리했다.
