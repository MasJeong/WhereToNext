# Changes made

- Reworked `landing-page.tsx` into a centered single-CTA landing with concise Korean copy.
- Rebuilt `hero-animation.tsx` as a minimal monochrome illustration with reduced-motion-safe CSS animation hooks.
- Simplified `step-question.tsx` and `progress-bar.tsx` for one-question-per-screen spacing and a plain progress bar.
- Expanded `result-page.tsx` to support a large primary destination summary, tags, concise facts, and lighter action layout.
- Reworked `home-experience.tsx` result rendering to keep recommendation logic intact while replacing dense cards with a primary hero + compact secondary list and existing save/share/compare behavior.
- Added white-first funnel tokens and small animation rules in `globals.css`.
