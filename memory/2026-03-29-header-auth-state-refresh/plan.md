# 한눈에 보기

- 목표: 로그인 후에도 헤더가 계속 `로그인`으로 보이는 문제를 해결하고, 현재 상태에 맞는 행동만 보이도록 상단 액션을 정리한다.
- 범위: 상단 헤더 인증 액션, 세션 연동 방식, 관련 e2e 검증을 포함한다.
- 제외: 로그인 공급자별 인증 로직 자체 변경은 하지 않는다.

## 계획

1. 헤더가 세션을 반영하지 못하는 원인을 확인한다.
2. 상단 액션을 세션 기반 클라이언트 컴포넌트로 분리한다.
3. 로그인 후 `로그아웃`이 노출되는지 e2e로 확인한다.
